Media-Server
---

A simple media server running a mediasoup SFU, signalling and an observer.

This package uses a Yarn `resolutions` override to map `@observertc/report-schemas-js` to `@observertc/schemas@2.2.0`, because the original `report-schemas-js@2.2.10` tarball is no longer available in the npm registry.


